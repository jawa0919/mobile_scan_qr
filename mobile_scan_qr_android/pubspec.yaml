name: mobile_scan_qr_android
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  mobile_scan_qr_platform_interface:
    path: ../mobile_scan_qr_platform_interface/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    implements: mobile_scan_qr
    platforms:
      android:
        package: com.example.mobile_scan_qr_android
        pluginClass: MobileScanQrAndroidPlugin
        dartPluginClass: MobileScanQrAndroid
